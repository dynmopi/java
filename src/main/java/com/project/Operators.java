package com.project;
// 3
public class Operators {
    public static void main(String[] args) {
        // operações matemáticas

        int x = 10;
        int y = 5;
        // operador de soma. Note que, se fosse strings, seria uma concatenação
        int z = x + y;
        System.out.println(z); // imprime 15

        // é possível somar valores e variáveis
        System.out.println(x + 15);

        // existem os seguintes grupos de operadores em Java
        /*
            Arithmetic operators
            Assignment operators
            Comparison operators
            Logical operators
            Bitwise operators
        */
    /* arithmetic operators.
        + soma (x + y)
        - subtração (x - y)
        * multiplicação (x * y)
        / divisão (x / y)
        % módulo (x % y) -> retorna o resto da divisão.
        ++ incremento (x + 1)
        -- decremento (x - 1)

        assignment operators
        =
        +=
        -=
        *=
        /=
        %=

        comparison operators

        == igual
        != diferente
        > maior que
        < menor que
        >= maior e igual a
        <= menor e igual a

        logical operators
        &&  logico "e"
        || logico "ou"
        ! logico "nao"
    */
    }

}
